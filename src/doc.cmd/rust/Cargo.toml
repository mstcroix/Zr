[package]
name        = "doc-cmd"
version     = "0.1.0"
edition     = "2021"
description = "Command database — lookup, record, replay"

[[bin]]
name = "doc-cmd"
path = "src/main.rs"

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
